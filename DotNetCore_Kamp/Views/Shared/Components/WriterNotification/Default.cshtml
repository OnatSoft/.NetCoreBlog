
<!--********* Burası Yazar Paneli üst menüde yer alan Bildirimler View Sayfası ********-->

@using EntityLayer.Concreate
@model List<Notification>

<a class="nav-link count-indicator dropdown-toggle" id="notificationDropdown" href="#" data-toggle="dropdown">
    <i class="mdi mdi-bell-outline"></i>
    <span class="count-symbol bg-danger"></span>
</a>

<div class="dropdown-menu dropdown-menu-right navbar-dropdown preview-list" aria-labelledby="notificationDropdown">
    <h6 class="p-3 mb-0"> Bildirimler </h6>
    @foreach (var item in Model.Where(x => x.Status == true).OrderByDescending(x => x.ID).Take(6))
    {
        <div class="dropdown-divider"></div>
        <a class="dropdown-item preview-item">
            <div class="preview-thumbnail">
                <div class="@item.NotificationColor">
                    <i class="@item.NotificationTypeSymbol"></i>
                </div>
            </div>
            <div class="preview-item-content d-flex align-items-start flex-column justify-content-center">
                <h6 class="preview-subject font-weight-normal mb-1"> @item.NotificationType </h6>
                <p class="text-gray ellipsis mb-0" title="@item.Details"> @item.Details </p>
            </div>
        </a>
    }

    <div class="dropdown-divider"></div>
    <h6 class="p-3 mb-0 text-center"><a class="text-dark" href="/Notification/AllNotification/"> Bildirim Listesi </a></h6>
</div>